interface:
  display_name: "Script Forge"
  short_description: "Write readable, stepwise scientific analysis scripts."
  brand_color: "#2563EB"
  default_prompt: "Use $script-forge to plan with a four-part checklist, then write a readable analysis script with clear Step sections and no redundant exploratory code."

policy:
  allow_implicit_invocation: true
